// Code generated by openapi sdkgen; DO NOT EDIT.

package config

import (
	"time"

	goconfig "github.com/gookit/config/v2"
)

// Config holds the configuration for the {{.Provider.DisplayName}} API client.
type Config struct {
{{- range .Config.Fields}}
	// {{.Name}} is configured via "{{.ConfigKey}}"
	{{.Name}} {{.Type}}
{{- end}}
}

// DefaultConfig returns the default configuration for the {{.Provider.DisplayName}} SDK.
func DefaultConfig() *Config {
	return &Config{
{{- range .Config.Fields}}
{{- if .Default}}
		{{.Name}}: goconfig.{{.GookitFunc}}("{{.ConfigKey}}", {{.Default}}),
{{- else}}
		{{.Name}}: goconfig.{{.GookitFunc}}("{{.ConfigKey}}"),
{{- end}}
{{- end}}
	}
}
